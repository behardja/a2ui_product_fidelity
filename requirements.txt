# Core agent + A2UI + A2A
google-cloud-aiplatform[agent_engines,adk]>=1.148.0
google-genai>=1.27.0
a2a-sdk>=0.3.13,<0.4    # 1.x reorganized a2a.types (DataPart/TextPart moved); stay on 0.3.x like the samples
a2ui-agent-sdk>=0.2.1

# Eval wrapper (vendored evaluation_wrapper/) needs the new vertexai eval client + pandas
vertexai>=1.148.0
pandas>=2.0.0

# GCS + images
google-cloud-storage>=2.0.0
Pillow>=10.0.0

# Server / misc
uvicorn>=0.30.0
click>=8.1.0
python-dotenv>=1.0.0
jsonschema>=4.0.0
json5>=0.9.0            # lenient fallback for LLM-emitted A2UI JSON (trailing commas, quotes)

# Only needed for deploy.py (deferred)
httpx
requests
